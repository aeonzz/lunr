{
  "github": {
    "enabled": false
  },
  "buildCommand": "cd ../.. && pnpm turbo run build --filter=server",
  "outputDirectory": "dist",
  "installCommand": "cd ../.. && pnpm install",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api" }
  ]
}
